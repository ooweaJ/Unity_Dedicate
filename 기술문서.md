# Unity 멀티플레이어 아레나 PvP — 기술문서

> **장르:** 브롤스타즈류 팀 아레나 PvP
> **엔진:** Unity (Mirror Networking)
> **발표 대상:** 포트폴리오

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 장르 | 3인칭 아레나 팀 PvP (Brawl Stars류) |
| 플레이어 수 | 최대 4명 (2v2) |
| 플랫폼 | PC + 모바일 (동시 지원) |
| 핵심 기술 | Unity, Mirror, KCP, Node.js, MySQL |

### 한 줄 소개
> 전용 배틀 서버를 직접 설계·구현한 Unity 멀티플레이어 아레나 게임.  
> 서버 권위(Server Authority) 구조로 치트를 방지하고, PC·모바일 입력을 인터페이스로 추상화해 단일 코드베이스로 운용.

---

## 2. 기술 스택

### 클라이언트
- **Unity** — 게임 엔진
- **Mirror Networking** — 멀티플레이어 네트워크 라이브러리
- **KCP Transport (kcp2k)** — UDP 기반 저지연 전송 프로토콜
- **Unity Timeline** — 가챠 연출

### 서버
- **Dedicated Server** — Unity 배치모드(BatchMode)로 직접 실행
- **Node.js + MySQL** — REST API 서버 (유저/캐릭터/전투 결과 저장)

### 아키텍처 패턴
- Server Authority (스탯·쿨다운 서버 이중 검증)
- Interface 기반 입력 추상화 (`IPlayerInputProvider`)
- ScriptableObject 기반 데이터 분리 (로비용 / 전투용)

---

## 3. 전체 시스템 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                      클라이언트                       │
│  Login → Boot → Lobby(로비서버 연결) → Battle씬       │
└──────────────┬──────────────────────────────────────┘
               │ Mirror / KCP
     ┌─────────┴──────────┐
     │                    │
┌────▼────┐         ┌─────▼─────┐
│ 로비 서버 │         │ 배틀 서버  │
│ Port 7777│         │ Port 7778 │
│(Matching)│         │(Game Loop)│
└────┬────┘         └─────┬─────┘
     │                    │
     └────────┬───────────┘
              │ HTTP REST
         ┌────▼────┐
         │API 서버  │
         │Node.js  │
         │+ MySQL  │
         └─────────┘
```

**핵심 설계 결정:** 로비 서버와 배틀 서버를 물리적으로 분리.  
배틀 서버는 Unity 배치모드로 직접 배틀씬에서 시작해 오버헤드를 최소화.

---

## 4. 서버 구조 상세

### 로비 서버 (Port 7777)
- `LobbyNetworkPlayer` — 접속 플레이어 관리
- 매치메이킹 큐 (`matchQueue`) — 4명 모이면 자동 배틀 서버 연결
- 매치 취소(`CmdCancelMatch`) 지원

### 배틀 서버 (Port 7778)
- `CustomNetworkManager` — 명령줄 인자(`-port`, `-serverType`)로 포트·타입 결정
- `MyAuthenticator` — 연결 시 클라이언트 데이터 검증
- `BattleNetworkPlayer` — 팀 배정, 닉네임, 선택 캐릭터 관리
- `BattleManager` — 전투 상태머신 (`WaitingForPlayers → InProgress → Ended`)

### 배틀 서버 라이프사이클
```
1. 4명 접속 완료
2. BattleManager.StartBattle() → InProgress
3. 180초 타이머 or 팀 전멸 → EndBattle()
4. RpcShowResult → 클라이언트 결과창 표시
5. 로비 서버 재접속 → 포트 반환
```

---

## 5. 인증 & 스탯 검증 (Anti-Cheat)

```
클라이언트                          서버
    │                                │
    ├─ MyAuthenticator               │
    │   StatUtils.Calculate()        │
    │   → AuthRequestMessage ───────►│
    │                                ├─ 서버에서도 StatUtils.Calculate()
    │                                ├─ 수치 불일치 → 연결 거부
    │                                ├─ 검증 통과 → CharacterSpawner 실행
```

- `StatUtils.Calculate(CharacterRawData, level, equippedItemIds)` — **스탯 계산의 단일 진입점**
- 클라이언트가 보낸 스탯을 서버에서 재계산해 비교 → 조작 불가

---

## 6. 전투 시스템

### 스킬 구조
| 슬롯 | 키 (PC) | 설명 |
|------|---------|------|
| 0 | 마우스 좌클릭 | 기본 공격 |
| 1 | Q | 스킬1 (슈퍼) |
| 2 | E | 스킬2 (가젯) |

각 슬롯은 `SkillData(cooldown + List<SkillAction>)`로 구성 → **Dash → Melee 같은 조합 액션** 자유롭게 설계 가능

### 쿨다운 이중 검증
```
클라이언트 쿨다운 (즉각 반응, UX용)
    + 서버 쿨다운 (실제 판정, 치트 방지)
```
- 입력 버퍼(0.15초) 보존 → 쿨다운 직전 입력도 자연스럽게 실행

### 공격 타입
| 타입 | 설명 |
|------|------|
| Melee | 근거리 히트박스 판정 |
| Projectile | 발사체 (직선/반사/폭발) |
| Dash | 돌진 + 이동 억제 |

### 발사체 종류
- `ProjectileBase` — 기본 직선 발사체
- `BouncingProjectile` — 벽 반사 발사체
- `ExplosiveProjectile` — 범위 폭발 발사체

---

## 7. 전투 판정 & 결과

### 승패 조건
1. **즉시 종료:** 한 팀이 전멸 → 나머지 팀 승리
2. **타임오버 (180초):** 팀별 HP 비율 합산 → 높은 팀 승리 (오차 1% 이하면 무승부)

### 결과 데이터 추적 (서버)
- `킬 / 데스 / 딜량` — 플레이어별 실시간 누적
- `ExpCalculator.Calculate()` — 결과 기반 경험치 산출
- `ExpCalculator.RankDelta()` — 랭크 포인트 증감 산출

### 결과 전달 흐름
```
서버 EndBattle()
  → scoreService.ReportMatchResult() (백엔드 API)
  → ClientRpc RpcShowResultAndPreload() (모든 클라이언트 결과창)
  → 확인 버튼 or 10초 후 → 로비 복귀
```

---

## 8. 캐릭터 데이터 구조

### ScriptableObject 분리 설계

```
CharacterRawData (CharacterTableSO)   ←── 로비 · UI · 서버인증용
├── id, name, icon
├── baseHp / Atk / Def
├── 강화 배율 (atkUpgradeBonus 등)
├── CharacterType
└── battleData ──────────────────────► CharacterDataSO  ←── 전투 메카닉 전용
                                        ├── weaponMeshPrefab
                                        ├── basicAttack (SO)
                                        ├── skill1Attack (SO)
                                        └── skill2Attack (SO, nullable)
```

**장점:** 전투용 데이터 변경이 로비 UI에 영향 없음. 책임 분리 명확.

### CharacterUIModel
- 로비 UI 전용 뷰모델
- `StaticData(CharacterRawData) + ServerData` 조합
- `StatUtils` 내장 → UI에서 스탯 직접 계산 가능

---

## 9. 인벤토리 & 강화 시스템

### 기능
- 아이템 드래그&드롭 장착 / 해제
- 캐릭터 강화 (EnhancePanel) / 초월 (TranscendPanel)
- 장착 아이템 기반 스탯 자동 반영 (`StatUtils.Calculate`)

### 데이터 흐름
```
로그인 응답 JSON
  → PlayerDataManager.ApplyUserData()
    → PlayerInventory (런타임 인벤토리)
      → CharacterUIModel (UI 바인딩)
        → InventoryUI 갱신
```

### 아이템 응답 포맷
```json
{ "userData": { "id", "username", "level", "gold", "characters", "items" } }
```

---

## 10. 가챠 시스템

- `GachaContext` — 가챠 로직 총괄 (확률·보상 산출)
- `GachaRewardDatabase` — ScriptableObject 기반 보상 테이블
- `GachaTimelineController` — Unity Timeline으로 연출 제어
- `GachaResultUI` — 결과 카드 애니메이션 표시

---

## 11. 맵 생성 시스템

### 절차적 맵 구성
- `MapFloorGenerator` — 바닥 타일 배치
- `MapWallGenerator` + `WallGenerator` — 벽 배치 (충돌 포함)
- `BushGridGenerator` — 덤불 그리드 생성
- `BushZone` + `PlayerBushState` — 덤불 내 플레이어 은신 판정

### 스폰 시스템
- `SpawnManager` — 팀별 스폰 포인트 관리
- 매치 종료 후 `ResetSpawnCounts()` 호출 → 포트 재사용 지원

---

## 12. 모바일 지원

### 입력 추상화
```csharp
interface IPlayerInputProvider {
    Vector2 MoveInput { get; }
    bool AttackPressed { get; }
    bool Skill1Pressed { get; }
    bool Skill2Pressed { get; }
}
```
- **PC:** `PlayerInputHandler` (키보드 + 마우스)
- **모바일:** `MobileInputProvider` (가상 조이스틱 + 버튼)

`PlayerController`는 인터페이스만 참조 → **플랫폼별 코드 분기 없음**

### 모바일 전용 UI
- `VirtualMovementJoystick` — 이동 조이스틱
- `VirtualJoystickButton` — 공격/스킬 버튼
- `MobileCombatUI` — 모바일 전투 HUD

---

## 13. 기술적 하이라이트 (돋보이는 점)

| # | 항목 | 내용 |
|---|------|------|
| 1 | **서버 권위 구조** | 스탯·쿨다운 서버 이중 검증으로 클라이언트 조작 원천 차단 |
| 2 | **로비/배틀 서버 분리** | 배틀 서버를 독립 프로세스로 운용, 포트 동적 할당으로 다중 매치 지원 |
| 3 | **입력 인터페이스 추상화** | `IPlayerInputProvider`로 PC/모바일 코드 완전 통합 |
| 4 | **입력 버퍼 시스템** | 쿨다운 직전 입력 0.15초 보존 → 반응성 체감 대폭 향상 |
| 5 | **ScriptableObject 데이터 분리** | 로비용/전투용 데이터 이원화 → 컴파일 없이 밸런싱 가능 |
| 6 | **타임오버 HP 비율 판정** | 단순 생존자 수 대신 팀 HP 비율 합산으로 공정한 판정 |
| 7 | **절차적 맵 + 덤불 은신** | 맵 생성기 + BushZone 은신 시스템으로 전략적 공간 연출 |

---

## 14. 프로젝트 구조 (폴더)

```
Assets/_dev/Script/
├── Battle/          # BattleManager, 맵 생성, Bush 시스템
├── Combat/          # CharacterWeapon, 발사체, 이펙트
├── Data/            # ScriptableObject 데이터 클래스
├── Gacha/           # 가챠 로직·연출·UI
├── Inventory/       # 인벤토리·강화 로직·UI
├── Lobby/           # 로비 컨트롤러·UI
├── Login/           # 로그인 컨트롤러·UI
├── Player/          # PlayerController, CharacterStats, 입력
├── Server/          # NetworkManager, Authenticator, NetworkPlayer
├── Shop/            # 상점 로직·UI
└── UI/              # 공통 UI (데미지팝업, HP바, 상태이상)
```

